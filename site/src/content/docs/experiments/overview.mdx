---
title: Experiments
description: Evidence-labelled studies and working notes built with BrainlessLab, with explicit status, inferential unit, artifacts, and limitations.
---

This section contains studies and working notes built with BrainlessLab. It is not a list of
equally established “published results.” Every page declares an evidence record in its
front matter:

- `status`: exploratory, tuned, frozen, confirmed, promoted, or retired;
- `id` and protocol version;
- randomization unit and number of independent blocks;
- primary endpoint;
- artifact path;
- the limitation that most constrains interpretation.

The labels follow [Design a study](/core/design-study/). A traceable run is not
automatically confirmatory: code and seeds may be recoverable even when the same data were
used to tune parameters and interpret the result. A `tuned` record means development data
influenced parameter or regime selection; it is not a higher scientific-evidence claim than
an exploratory record.

```bash
julia --project=. experiments/run.jl --list
julia -t auto --project=. experiments/run.jl <name>
```

## Current evidence register

| Study | Status | Independent unit | Artifact | Main limitation |
| --- | --- | --- | --- | --- |
| [Tracking parameter sweep](/experiments/tracking-param-sweep/) | **tuned development** | seeded reservoir/task run; 100 per cell | two committed development bundles | selection and signature interpretation used the same grids |
| [Freeze-plasticity onset](/experiments/freeze-onset/) | **exploratory** | seeded reservoir/task run | no promoted bundle | sample count, uncertainty, and frozen protocol are not documented in an immutable artifact |
| [Social foraging](/experiments/social-foraging/) | **tuned development** | seeded multi-agent world; 3–8 per tuned cell | ad-hoc outputs only | morphology/environment were tuned on the reported seeds; no sham-information control |
| [Moving shoal vision sweep](/experiments/shoal-vision-sweep/) | **exploratory pilot** | matched seeded world block; 2 blocks | generated local run bundle | underpowered; stylized embodiment and recorder-grid movement diagnostics |
| [Criticality across tested perturbations](/experiments/criticality-control/) | **exploratory** | seeded reservoir or world; 3–10 per probe | exploratory scripts/tables | small ad-hoc probes and no fixed-weight confirmatory comparison |

There are currently no pages labelled `frozen`, `confirmed`, or `promoted`. That is a useful
state, not a failure: the existing material generates hypotheses and tests the platform,
while the new evidence contract prevents exploratory work from silently becoming
confirmation. `frozen` means the protocol is fixed and the sealed outcomes remain unopened;
`confirmed` means that protocol has been executed on the sealed blocks.

## What belongs here

An analysis over one recorded run belongs in the analysis registry. A protocol that
coordinates conditions across many runs belongs under `experiments/`. Keep protocols on the
public API so core refactors do not become study-specific.

Before a study can be promoted, its bundle must include the frozen protocol, resolved
configuration, full code/environment provenance, disjoint seed ledger, per-block data,
declared contrasts and inferential unit, analysis version, summary data, figure inputs,
representative-selection rule, and checksums. See `experiments/README.md` for the repository
checklist.

:::note[Stable model does not mean confirmed study]
The fixture-validated `:falandays` reservoir (`:falandays_base` is a compatibility alias) can
be used inside an exploratory task,
body, analysis, or protocol. Baseline implementation status and study evidence status are
separate.
:::
